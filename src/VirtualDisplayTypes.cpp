// File: src/VirtualDisplayTypes.cpp
#include "pch.h"
#include "VirtualDisplayTypes.h"

//
// Purpose:
//  - This translation unit intentionally contains no runtime code.
//  - Keep this small .cpp so the build system has a stable TU to compile even when
//    VirtualDisplayTypes.h is header-only. It documents intent and is safe to remove
//    if you confirm the header is fully header-only and no build target expects this file.
//
// If VirtualDisplayTypes.h declares non-inline symbols that require out-of-line
// definitions (eg. const/non-const static members, explicit template instantiations,
// or non-inline functions), implement those definitions below.
//
// Example (do not enable unless the header declares these):
// namespace vdc {
//   // Out-of-line definition for a non-inline static member:
//   // const std::string VirtualDisplayTypes::someName = "value";
// }

// No-op ensures this TU compiles even if unused.